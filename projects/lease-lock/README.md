# Lease Lock

A deterministic in-memory lease/lock state model with expiry, renewal, release, and injected time.

Primary API: `LeaseLock` and `LeaseSnapshot`.

Suggested curriculum mapping: Parts 13, 23, 114–120.

> Complete TypeScript Full Mastery eBook: **https://ramsandesh.gumroad.com**
