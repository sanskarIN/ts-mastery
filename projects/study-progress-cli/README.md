# Study Progress CLI

A small dependency-free CLI companion project. Pass completed part numbers on the command line and it reports normalized progress across the 120-part series.

Example after building:

```bash
node dist/projects/study-progress-cli/src/index.js 1 2 3 4 5
```
