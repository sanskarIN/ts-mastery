# Generic Binary Heap

A reusable comparator-driven binary heap supporting arbitrary value types.

Primary API: `BinaryHeap<T>` with `push`, `pop`, `peek`, and `size`.

Suggested curriculum mapping: Parts 8–9, 16, 20, 94.

> Complete TypeScript Full Mastery eBook: **https://ramsandesh.gumroad.com**
