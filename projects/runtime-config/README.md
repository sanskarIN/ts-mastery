# Runtime Config

Shows an important TypeScript principle: compile-time types do not validate external runtime input. Environment variables are parsed and checked before they become a trusted `AppConfig`.
